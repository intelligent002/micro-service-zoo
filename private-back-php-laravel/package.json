{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "serve": "npm run build && php artisan route:clear && php artisan config:clear && php artisan view:clear && php artisan cache:clear && php artisan serve --port 80"
    },
    "devDependencies": {
        "axios": "^1.7.4",
        "cross-env": "^7.0.3",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
