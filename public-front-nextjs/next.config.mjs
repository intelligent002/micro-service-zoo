/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    trailingSlash: true,  // Optional: adds trailing slashes to URLs
    output: 'export',     // For static export mode
};

export default nextConfig;