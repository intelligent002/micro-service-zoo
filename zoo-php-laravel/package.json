{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "npm run refresh && npm run serve",
        "serve": "php artisan serve --port 8000",
        "refresh": "php artisan route:clear && php artisan config:clear && php artisan view:clear && php artisan cache:clear"
    },
    "devDependencies": {
        "axios": "^1.7.4",
        "cross-env": "^7.0.3",
        "laravel-vite-plugin": "^1.3",
        "vite": "^7.0.0"
    }
}
